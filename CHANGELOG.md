## 0.1.0 (Unreleased)

- Initial release
- Railtie: auto-configures `Workflow.configuration.logger` from `Rails.logger`
- Generator: `workflow:action` — generates action classes with expects/promises
- Generator: `workflow:organizer` — generates organizer classes with step composition
